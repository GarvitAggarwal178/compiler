// Comment whose text contains a '.' character -- must not be mistaken for
// a clause terminator by a naive scanner.
.decl q(a:number)
.input q
.decl p(a:number)
.output p
// this comment ends with a sentence. and another. and a third one.
p(X) :- q(X). // trailing comment with a period too.
