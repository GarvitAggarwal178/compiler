// Lexical: unicode identifiers -- relation name and variable name both use
// non-ASCII letters.
.decl données(a:number)
.input données
.decl résultat(a:number)
.output résultat
résultat(Élève) :- données(Élève).
