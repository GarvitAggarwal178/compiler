// Comment placed inside a rule body, between two literals.
.decl q(a:number)
.input q
.decl r(a:number)
.input r
.decl p(a:number)
.output p
p(X) :- q(X), // comment right after a literal, before the comma's peer
  r(X).
