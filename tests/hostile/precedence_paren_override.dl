// Precedence: parens override default precedence. Expected: (2+3)*4 = 20,
// not 2+(3*4)=14 (which is what precedence_add_mul.dl checks unparenthesized).
.decl p(a:number)
.output p
p(X) :- X = (2 + 3) * 4.
