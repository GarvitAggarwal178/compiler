{
  "_note": "NIGHT-BATCH-04 B. Pre-registered before any measurement run against this corpus -- committed alongside the four .dl programs, before harness/night04_b_cone_gate.py exists. Mirrors BENCHMARK_FAMILY/SCALE_POINTS.json's own convention (same generator style, gen_culprit_cycle_facts' n/target_base/target_e pattern) applied to harness/fixtures_lib.py's gen_cone_corpus_facts.",
  "programs": ["cc_cone_only", "cc_sibling_emptycone", "cc_both", "cc_cone_proper_subset"],
  "generator": "gen_cone_corpus_facts",
  "params_per_point": ["seed", "n", "target_base", "target_e"],
  "seed_base": 20260827500,
  "note": "seed = seed_base + n, same across all four programs at a given n (identical base/e/blocked/gate/chain/sibling fact content at each scale point -- only the .dl files differ in which relations they read). Same blocked_fraction/gate_fraction/gate_edges/sibling_edges defaults as gen_cone_corpus_facts.",
  "points": [
    {"n": 20, "target_base": 30, "target_e": 30},
    {"n": 50, "target_base": 75, "target_e": 75},
    {"n": 100, "target_base": 150, "target_e": 150}
  ]
}
